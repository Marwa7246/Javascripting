const fruit = 'orange';
const x = fruit.length;
if (x <= 5) {
    console.log('The fruit name has more than five characters.');

} else {
    console.log('The fruit name has more than five characters.');
}